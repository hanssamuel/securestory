PORT=8002
DATABASE_URL=postgres://securestory:REPLACE_WITH_STRONG_PASSWORD@localhost:5432/securestory
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_VALUE

# Only needed if password-reset emails are enabled
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
APP_BASE_URL=http://localhost:5173
