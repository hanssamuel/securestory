services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: securestory
      POSTGRES_PASSWORD: securestory
      POSTGRES_DB: securestory
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
